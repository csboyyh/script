#/bin/bash

find . -name "*$1*" -exec rename 's/$1/$2/' {} \;
find . -name "*$1*" -exec rename 's/$1/$2/' {} \;

for file in `grep $1 -ri . | awk -F ':' '{print $1}'`
do
    echo "modify file:$file"
    sed -i "s/$1/$2/g" $file
done

